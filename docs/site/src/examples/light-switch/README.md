# Light switch preview

This source uses the unreleased `commands({ source })` API.
Published beta.14 and the current `@beta` package do not support it.

The `workspace:*` dependency deliberately requires the integrated candidate.
This archive is preview source, not a standalone public-package install.
Do not replace that dependency with beta.14.

The documentation review validates these exact component, CSS and HTML files
in a separate project with locally packed candidate packages.
A version-pinned standalone download will accompany the supporting beta release.
