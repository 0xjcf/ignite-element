---
title: Sources
description: Choose a source adapter, derive values, and send commands to your source.
---

import { Code } from '@astrojs/starlight/components';
import redux from '../../../../../../scripts/__tests__/fixtures/handbook/redux.ts?raw';
import mobx from '../../../../../../scripts/__tests__/fixtures/handbook/mobx.ts?raw';

Your source owns state and behavior.

Pass it to `igniteCore` through the entrypoint for your source library.

Use `states` to derive values for the view and `commands` to call the source.

## Choose a source adapter

An Ignite source adapter connects your source to Ignite's observation and command APIs.

| Source | Import `igniteCore` from |
| --- | --- |
| XState | `ignite-element/xstate` |
| Redux | `ignite-element/redux` |
| MobX | `ignite-element/mobx` |
| Actor-Web | `ignite-element/actor-web` |

Network requests, storage, timers, and other I/O stay in your source or application.

## What `states` receives

The callback input depends on the source adapter.

| Source | Input to `states` |
| --- | --- |
| XState | The actor snapshot, including `context`, `matches(...)`, and `can(...)` for state machines. |
| Redux | The store's `getState()` result, or the selected slice state when you pass a slice. |
| MobX | The result of `toJS(store)`, rather than the observable instance itself. |
| Actor-Web | An extended state combining the source snapshot, its context fields, and transport status. |

The name `snapshot` in the examples is a callback parameter, not a shared shape across all sources.

## Command target

Use `commands: ({ source })` to access the resolved command interface.
Configuration `source` accepts the adapter's supported instance, factory, or definition.
The callback receives the command target for the active instance, not necessarily the original configuration object.

Destructuring can give that target a local name: `{ source: actor }` for XState or `{ source: store }` for Redux and MobX.
These aliases keep inference; they do not add properties to the command context.

## XState

Install Ignite and XState:

```sh title="Terminal"
pnpm add ignite-element@beta xstate
```

The [Getting started light switch](/ignite-element/#build-a-component) passes a machine definition to `igniteCore`.

Each mounted light switch gets its own actor.

To share state between views, pass an existing started actor instead.

XState machine states describe behavior; Ignite's `states` callback derives values for views.

Keep guards, invoked services, cancellation, retries, and persistence in XState or the application.

## Redux

Install Ignite and Redux Toolkit:

```sh title="Terminal"
pnpm add ignite-element@beta @reduxjs/toolkit
```

Create `redux.ts` to connect a counter store:

<Code code={redux} lang="ts" title="redux.ts" />

The `states` callback reads `snapshot.count` because this store uses the counter reducer directly.

The `increment()` command dispatches the slice's action to the store.

For a combined store, read its root shape, such as `snapshot.counter.count`.

Passing a store shares that store between views.

Passing a Redux slice instead creates isolated state for each element.

## MobX

Install Ignite and MobX:

```sh title="Terminal"
pnpm add ignite-element@beta mobx
```

Create `mobx.ts` to connect an observable counter:

<Code code={mobx} lang="ts" title="mobx.ts" />

The `states` callback reads the observable data copied by `toJS`.

Do not assume methods or computed getters are fields in that result.

The `increment()` command calls `store.increment()` on the observable instance.

Passing an observable instance shares it between views.

For an isolated observable per element, use `source: () => new Counter()` with `adapter: "mobx"`.

The explicit adapter identifies the factory without calling it to guess its type.

## Optional integrations

These integrations are not required for XState, Redux, or MobX:

- [Actor-Web adapter](/ignite-element/guides/actor-web/): project an existing distributed runtime into a view.
- [Plain-controller interoperability](/ignite-element/guides/plain-controllers/): adapt an existing TypeScript controller to the supported source protocol.

Actor-Web retains ownership of admission, authorization, execution receipts, replay, transport, and shutdown.

Find these pages again in the sidebar's __Guides__ group.

## Where next

- [Views](/ignite-element/handbook/views/): render the values and commands exposed by your core.
- [Ownership & cleanup](/ignite-element/handbook/ownership/): choose shared or isolated state and place cleanup at its owner boundary.

At final application shutdown, call `core.dispose()` to release Ignite-owned resources.

The application remains responsible for shutting down any source it created.
