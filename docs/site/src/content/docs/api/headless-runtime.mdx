---
title: Headless runtime
description: The agent runtime API on the igniteCore return — execute, canExecute, getSnapshot, getStates, getSchema, on, watchSnapshot, watchStates, and record.
sidebar:
  label: Headless runtime
  order: 4
---

Every `igniteCore(...)` registration is also a **headless runtime**. The same value you call with a tag name to register a custom element exposes methods that drive and inspect the component contract without a DOM — the surface agents and tests use before any selector is involved.

The object passed to `igniteCore(config)` is executable authoring input. It can contain source actors or factories, callbacks, selectors, effects, and other application-only values. It is not the serialized runtime contract. Use `runtime.getSchema()` for the sole public compiled, JSON-safe Ignite blueprint.

```ts
import { igniteCore } from 'ignite-element/xstate';

const counter = igniteCore({
  source: counterMachine,
  states: (snapshot) => ({
    isOn: snapshot.matches('on'),
  }),
  commands: ({ actor }) => ({
    toggle: () => actor.send({ type: 'TOGGLE' }),
  }),
  events: (event) => ({
    toggled: event<{ isOn: boolean }>(),
  }),
  effects: ({ emit, select }) => {
    const isOn = select((snapshot) => snapshot.matches('on'));
    if (!isOn.changed) return;
    emit({ type: 'toggled', isOn: isOn.current });
  },
});

// Register a DOM projection later — the runtime is available immediately.
const { snapshot, states, events } = await counter.execute({ command: 'toggle' });
const isToggleAvailable = counter.canExecute('toggle');
const current = counter.getSnapshot();
const currentStates = counter.getStates();
const schema = counter.getSchema();
```

## Discovery layers

| Layer | Contract |
| --- | --- |
| Authoring | `igniteCore(config)` accepts executable application input, including source ownership and callbacks. |
| Blueprint | `getSchema()` is the sole public compiled JSON-safe description of commands, declared events, snapshot, and states. |
| Focused reads | `getSnapshot()`, `getStates()`, and `canExecute(...)` answer one live question at a time. |
| Subscriptions | `on(...)`, `watchSnapshot(...)`, and `watchStates(...)` keep long-lived consumers synchronized. |
| Private machinery | Each projection validation and commit attempt captures one coherent inspection inside Ignite; it is not a public API. |

Separate focused getter calls are non-atomic. A source transition between calls can make them observe different revisions; use subscriptions and `execute()` results for ongoing synchronization.

## Methods

### `execute({ command, input? })`

Runs a command by name and resolves at command acknowledgement, after the command window closes and the transition result is available. Effects are synchronous; asynchronous environmental work belongs to the source runtime and is not part of this acknowledgement window. The input type is inferred from the command function's first parameter, so commands that take no argument omit `input`.

**Returns** `Promise<{ snapshot, states, events }>` — one coherent post-command snapshot/states pair plus the ordered events emitted by that command window (an `IgniteAgentExecutionResult`).

```ts
const result = await counter.execute({ command: 'toggle' });
```

The shape:

```ts
// IgniteAgentExecutionResult<State, Events>
{
  snapshot: { value: 'on', context: {} },
  states: { isOn: true },
  events: [{ type: 'toggled', isOn: true }],
}
```

`events` is the same ordered stream the runtime emitted for that transition, so assertions stay deterministic and align with the events delivered to `on(...)` listeners.

For sources with a native emit stream (XState and Actor-Web today), domain events the **source** emits during the command window are captured too as flat `{ type, ...fields }` members, independent of the `events:` declaration. See [declared vs source-emitted events](/ignite-element/concepts/the-ignite-model/#commands-events-and-effects).

### `canExecute(commandName)`

Returns whether a command is available for the current snapshot. Commands without
a `canExecute` predicate are available by default; commands with
[`command(..., { canExecute })`](/ignite-element/api/command-metadata/#availability-predicates)
evaluate that predicate on demand.

**Returns** `boolean`.

```ts
const canToggle = counter.canExecute('toggle');
```

Unknown command names throw the same unknown-command error shape as
`execute(...)`.

### `getSnapshot()`

Returns the current raw snapshot of the underlying source.

**Returns** `State` — the underlying source snapshot (the XState snapshot, the Redux state, or the MobX observable value).

```ts
const snapshot = counter.getSnapshot();
```

The pre-stable `getState()` alias was removed at v3.0.0 — use `getSnapshot()`.

### `getStates()`

Returns the current derived states — the plain object your optional `states(snapshot)` callback derives from the native snapshot.

**Returns** the inferred states object, or one stable `{}` when no `states` callback is configured.

```ts
const states = counter.getStates();
```

### `on(eventName, handler)`

Subscribes to a public emitted event. The handler receives the flat emitted event member.

**Returns** a subscription — `{ unsubscribe(): void }`. Call `unsubscribe()` to stop listening.

```ts
const subscription = counter.on('toggled', (event) => {
  console.log(event.isOn);
});

subscription.unsubscribe();
```

Use `on(...)` for the public events your component emits through `effects`. When the source has a native emit stream (XState and Actor-Web today), `on(...)` also receives the source's emitted domain events as the same flat `{ type, ...fields }` shape, and the names are typed from the source's `Emitted` union. (The pre-stable `subscribe(eventName, handler)` alias was removed at v3.0.0 — use `on`.)

### `watchSnapshot(handler)`

Subscribes to **raw snapshot** updates. The handler receives `(snapshot, prevSnapshot)` on every source update.

**Returns** a subscription — `{ unsubscribe(): void }`.

```ts
const subscription = counter.watchSnapshot((snapshot, prevSnapshot) => {
  console.log(prevSnapshot.value, '->', snapshot.value);
});
```

The pre-stable `watch(handler)` alias was removed at v3.0.0 — use `watchSnapshot()`.

### `watchStates(handler)`

Subscribes to **derived states** updates. The handler receives `(states, prevStates)` for every delivered source observation; this API does not add deep-equality filtering.

**Returns** a subscription — `{ unsubscribe(): void }`.

```ts
const subscription = counter.watchStates((states, prevStates) => {
  console.log(prevStates.isOn, '->', states.isOn);
});
```

### `getSchema()`

Returns the sole public compiled, JSON-safe Ignite blueprint describing the component's commands, declared public events, and current state. Command metadata declared with [`command(...)`](/ignite-element/api/command-metadata/) is included here.

"Blueprint" is Ignite vocabulary, not a claim that this object is a formal JSON Schema document. A command's `input` metadata may use JSON-Schema-like fragments, but the complete blueprint is an Ignite discovery contract.

**Returns** `{ commands, events, snapshot, states }` — a JSON-serializable `IgniteAgentSchema`: `commands` maps each name to its metadata (`{}` when none), `events` is the array of declared event descriptors such as `{ type }`, and `snapshot` and `states` are one coherent current observation. Commands with an availability predicate include `gated: true`; call `canExecute(commandName)` for the current boolean result.

The blueprint excludes source actors and factories, effects, callbacks, selectors, registries, projection bindings, committers, and executable model-authored content.

```ts
const schema = counter.getSchema();
```

The shape:

```ts
// getSchema() returns:
{
  commands: {
    toggle: {
      description: 'Toggle the panel open state.',
    },
    setMode: {
      description: 'Switch the device mode.',
      input: {
        type: 'string',
        enum: ['auto', 'manual', 'off'],
      },
    },
    armAlarm: {
      description: 'Arm the alarm when every zone is closed.',
      gated: true,
    },
  },
  events: [{ type: 'toggled' }, { type: 'alarm-armed' }],
  snapshot: {
    value: 'idle',
    context: {
      mode: 'auto',
      zonesOpen: 0,
      armed: false,
    },
  },
  states: {
    mode: 'auto',
    canArmAlarm: true,
    statusLabel: 'Ready',
  },
}
```

How tools usually consume it:

1. Read `commands` to decide which actions exist and whether any carry input schemas or descriptions.
2. Use `input` to shape payloads before calling `execute({ command, input? })`.
3. Treat `gated: true` as "availability is dynamic" and call `canExecute(commandName)` before offering or invoking that command.
4. Use `snapshot` when the raw source state matters, and `states` when the derived read model is the better contract.
5. Observe outcomes with `execute().events`, `on(...)`, or `watchStates(...)` instead of assuming a command succeeded silently.

Example runtime flow:

```ts no-check
// Install subscriptions before the initial read so a transition during the
// bootstrap handoff is observed.
const eventSubscription = panel.on('alarm-armed', (event) => {
  console.log('Alarm status:', event);
});

const statesSubscription = panel.watchStates((states) => {
  console.log('Status label:', states.statusLabel);
});

const schema = panel.getSchema();
const armMetadata = schema.commands.armAlarm;

if (armMetadata.gated && !panel.canExecute('armAlarm')) {
  throw new Error('Alarm cannot be armed while a zone is open.');
}

const result = await panel.execute({ command: 'armAlarm' });
console.log('Command events:', result.events);

eventSubscription.unsubscribe();
statesSubscription.unsubscribe();
```

This separation is intentional:

- `getSchema()` tells you what the runtime exposes right now in JSON-safe form.
- `canExecute()` answers dynamic command availability.
- `execute()` performs the transition and returns the resulting snapshot, derived states, and emitted events.
- `on(...)` and `watchStates(...)` keep long-lived observers in sync across bootstrap and after the initial schema read.

Ignite does not expose a second `getBlueprint()` alias, a public `inspect()` method, or a public inspection-bundle type. Internally, each projection validation and commit attempt captures one private coherent inspection, so the document and behavior facts for that attempt come from the same captured revision. An asynchronous external commit is not transactional with source transitions that occur later; the captured inspection is not part of the headless API.

:::note[Dynamic events are not listed]
`events` enumerates only the names **declared** in the `events:` map. Events emitted by XState or Actor-Web sources are a compile-time union with no runtime registry, so they don't appear in the schema — observe them via `on(...)` and `execute().events` instead.
:::

### `record(name)`

Starts a named [story](/ignite-element/api/testing-dsl/#stories) recorder over the same runtime.

**Returns** a [story](/ignite-element/api/testing-dsl/#stories) — an object that records command, native snapshot, derived states, and event activity as one ordered behavior trace (`execute`, `until`, `trace`, `lifecycle`, `summary`, `stop`). Its summary exposes `finalSnapshot` and `finalStates`, and derived-state trace entries use `kind: 'states'`.

```ts
const story = counter.record('toggles on');
```

## Determinism

The runtime seeds `prevSnapshot` on attach, never replays history, and runs `effects(...)` once per update in a stable order — which is what keeps `execute()` and the testing helpers reproducible. The full semantics are documented once in [Deterministic effects](/ignite-element/concepts/the-ignite-model/#deterministic-effects).

## Related

- [Build for agents](/ignite-element/guides/agent-runtime-v3/) — the behavior-first workflow these methods support.
- [Command metadata](/ignite-element/api/command-metadata/) — enrich `getSchema()` output.
- [Testing DSL](/ignite-element/api/testing-dsl/) — scenario and story helpers built on this runtime.
