# add igniteCore agent schema generation

## Source
Created with `fas create-task` on 2026-03-21.

## Problem
Complete Brief 8 by introducing getSchema() output for commands, events, and state in a JSON-serializable format suitable for agents and automation frameworks.

## Acceptance criteria
- The work is tracked in `.fas/TASKS.md`.
- The task has a clear implementation and verification plan before execution starts.
- The task is queued in `.fas/queue/tasks.json` for the runtime.
