# remove deprecated emit from commands and finalize command-only API

## Source
Created with `fas create-task` on 2026-03-21.

## Problem
Complete the final phase of Brief 1 by removing emit from command context after the migration path is in place, tightening public types, removing the deprecation shim, and updating docs/changelog for the breaking release boundary.

## Acceptance criteria
- The work is tracked in `.fas/TASKS.md`.
- The task has a clear implementation and verification plan before execution starts.
- The task is queued in `.fas/queue/tasks.json` for the runtime.
