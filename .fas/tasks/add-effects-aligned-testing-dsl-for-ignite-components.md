# add effects-aligned testing DSL for ignite components

## Source
Created with `fas create-task` on 2026-03-21.

## Problem
Complete Brief 9 by designing a deterministic testing API aligned with execute/getState/effects, including expectState and expectEvent patterns for headless component tests.

## Acceptance criteria
- The work is tracked in `.fas/TASKS.md`.
- The task has a clear implementation and verification plan before execution starts.
- The task is queued in `.fas/queue/tasks.json` for the runtime.
