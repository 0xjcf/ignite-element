---
"ignite-element": minor
---

Add `expectView(expected)` to the test DSL — assert the projected view (mirrors the runtime's `getView()`) alongside `expectState`. Accepts a deep-partial object match or a predicate over the view.
