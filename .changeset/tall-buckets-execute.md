---
"@ignite-element/core": minor
"@ignite-element/adapters": minor
"ignite-element": minor
---

Add command availability predicates through `command(fn, { canExecute })` and expose the headless runtime `canExecute(name)` query, with gated commands marked in `getSchema()`.
